spring:
  application:
   name: "configserver"
  profiles:
   # active: native
    active: git
  cloud:
    config:
      server:
        #native:
        # search-locations: "file:///Users//eazybytes//Documents//config"
        # search-locations: "classpath:/config"
        git:
          uri: "https://github.com/ApexUmrao/Config.git"
          search-paths:
            - ApexBank
          default-label: main
          timeout: 5
          clone-on-start: true
          force-pull: true

server:
  port: 8071
